import { StudioEmptyState } from "@/components/studio/StudioEmptyState";

export function ProjectOrganizationsSection() {
  return (
    <StudioEmptyState
      headline="Organizations coming soon"
      description="Factions, nations, and groups will live here as first-class project entities. For now, use setting reference slots for organization imagery."
    />
  );
}
